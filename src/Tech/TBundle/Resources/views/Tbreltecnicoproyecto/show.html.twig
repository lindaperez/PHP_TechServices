{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tbreltecnicoproyecto</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Dfecha</th>
                <td>{{ entity.dfecha|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Vdescripcioncambioest</th>
                <td>{{ entity.vdescripcioncambioest }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('Asignacion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('Asignacion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
