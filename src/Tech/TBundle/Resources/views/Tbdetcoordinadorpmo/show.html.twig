{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tbdetcoordinadorpmo</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Valias</th>
                <td>{{ entity.valias }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('AsignacionLider') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('AsignacionLider_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
