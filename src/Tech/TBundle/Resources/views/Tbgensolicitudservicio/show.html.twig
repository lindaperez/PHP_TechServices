{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tbgensolicitudservicio</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Itiposol</th>
                <td>{{ entity.fkIidEspSol }}</td>
            </tr>
            <tr>
                <th>Iespecificacion</th>
                
            </tr>
            <tr>
                <th>Dfechacreacion</th>
                <td>{{ entity.dfechaCreacion|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('SolicitudServicio') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('SolicitudServicio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
