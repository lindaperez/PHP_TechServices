{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tbgensolicitudservicio list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>

                <th>Dfechacreacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('SolicitudServicio_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>

                <td>{% if entity.dfechaCreacion %}{{ entity.dfechaCreacion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('SolicitudServicio_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('SolicitudServicio_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('SolicitudServicio_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
