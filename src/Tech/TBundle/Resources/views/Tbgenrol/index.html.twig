{% extends '::layout.html.twig' %}

{% block content %}
    <h1>Lista de Roles</h1>
<br><br>
    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Descripcion</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('Roles_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.vdescripcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('Roles_show', { 'id': entity.id }) }}">Ver</a>
                    </li>
                    <li>
                        <a href="{{ path('Roles_edit', { 'id': entity.id }) }}">Editar</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<br>
        <ul>
        <li>
            <a href="{{ path('Roles_new') }}">
                Crear Rol
            </a>
        </li>
    </ul>
    {% endblock %}
