{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tbgendetalle</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Vdescripcion</th>
                <td>{{ entity.vdescripcion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('Detalles') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('Detalles_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
    <br><br><br><br>
{% endblock %}
