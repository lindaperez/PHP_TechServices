{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tblogconexion</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Dfechaacceso</th>
                <td>{{ entity.dfechaAcceso|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('ReporteLogin') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('ReporteLogin_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
