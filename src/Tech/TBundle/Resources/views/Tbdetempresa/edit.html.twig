{% extends '::layout.html.twig' %}

{% block content -%}
<h1>Edición de Empresa</h1>
<br><br>
    <form action="{{ path('Empresa_create') }}" method="post" id="login">
 {{ form_start(edit_form) }}
        {{ form_errors(edit_form) }}
        
        {{ form_row(edit_form.pkIrif, {'label': 'Rif:'}) }}
        {{ form_row(edit_form.vdireccionFiscal, {'label': 'Dir-Fiscal:'}) }}
        <div style="display: none;">
        {{ form_row(edit_form.submit, { 'label': 'Actualizar' }) }}
    </div>
    <br>
    <button type="submit" class="sf-button">
            <span class="border-l">
                <span class="border-r">
                    <span class="btn-bg">Actualizar</span>
                </span>
            </span>
    </button>
    
   {{ form_end(edit_form) }}     

</form>
<br><br>

        <ul class="record_actions">
    <li>
        <a href="{{ path('Empresa') }}">
           Lista de Empresas
        </a>
    </li>
    <li>{{ form_row(delete_form.submit, { 'label': 'Eliminar' }) }} </li>
 
</ul>
 {% endblock %}
