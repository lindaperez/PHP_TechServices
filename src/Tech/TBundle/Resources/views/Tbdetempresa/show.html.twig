{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tbdetempresa</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Pkirif</th>
                <td>{{ entity.pkIrif }}</td>
            </tr>
            <tr>
                <th>Vdireccionfiscal</th>
                <td>{{ entity.vdireccionFiscal }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('Empresa') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('Empresa_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
