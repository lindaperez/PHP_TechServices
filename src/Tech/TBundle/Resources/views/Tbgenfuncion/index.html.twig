{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tbgenfuncion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Pkiidfuncion</th>
                <th>Vdescripcion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('Funciones_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.pkIidFuncion }}</td>
                <td>{{ entity.vdescripcion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('Funciones_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('Funciones_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('Funciones_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
