{% extends '::layout.html.twig' %}

{% block content %}
<br><br>
    <h1>Lista de Contratos Asociados</h1>
<br><br>
    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('UsuariosContratos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('UsuariosContratos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('UsuariosContratos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('UsuariosContratos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
