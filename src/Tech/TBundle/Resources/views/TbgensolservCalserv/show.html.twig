{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TbgensolservCalserv</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Icalsolpreg</th>
                <td>{{ entity.icalSolPreg }}</td>
            </tr>
            <tr>
                <th>Irespuesta</th>
                <td>{{ entity.irespuesta }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('CalidadServicio') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('CalidadServicio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
