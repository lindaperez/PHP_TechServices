{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tbgentiposolicitud</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Vnombretiposol</th>
                <td>{{ entity.vnombreTipoSol }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('Servicios') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('Servicios_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
