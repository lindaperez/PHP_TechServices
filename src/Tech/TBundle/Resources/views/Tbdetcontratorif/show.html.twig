{% extends '::layout.html.twig' %}

{% block content -%}
    <h1>Contrato </h1>
<br><br>
    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nro-Contrato</th>
                <td>{{ entity.pkInroContrato }}</td>
                
            </tr>
        </tbody>
    </table>
<br><br>
        <ul class="record_actions">
    <li>
        <a href="{{ path('Contratos') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('Contratos_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    
    <li>{{ form(delete_form) }}</li>
    
</ul>
    {# ... #}
 
{# ... #}

{% endblock %}
