{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Tbdetentrega list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Dfecha</th>
                <th>Icantidadentregada</th>
                <th>Vobservaciones</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('Entrega_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dfecha %}{{ entity.dfecha|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.icantidadentregada }}</td>
                <td>{{ entity.vobservaciones }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('Entrega_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('Entrega_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('Entrega_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
