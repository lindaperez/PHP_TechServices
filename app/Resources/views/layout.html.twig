{% extends "base.html.twig" %}

{% block head %}
    <link rel="stylesheet" href="{{ asset('bundles/acmedemo/css/demo.css') }}" />
    
{% endblock %}

{% block title 'Techtrol' %}

{% block body %}

<br><br>
    {% block content_header %}
        <ul id="menu">
            
            {% block content_header_more %}
            
            
    {{ parent() }}
    {% if app.user!=null %}
    <ul>Bienvenido, <strong>{{  app.user.vnombre }} {{  app.user.vapellido}}</strong> ::{{app.session.get('usuario_rol')}}
 ::{{app.session.get('usuario_tipo_rol')}} ::{{app.session.get('usuario_estatus_registro')}} <a href="{{ path('logout') }}">Logout</a></ul>
    
    {% else %}
    <li><a href="{{ path('login') }}">Login</a></li>  
    {% endif%}
                
        
            {% endblock %}
                
        </ul>

        <div style="clear: both"></div>
    {% endblock %}

    <div class="block">
        {% block content %}
        
{% endblock %}
</div>

{% endblock %}
